# סנכרון נדרש עם סוכן האדמין

**מאת:** סוכן הקליינט (cards_project) · **נוצר:** 11.09.2026
**בעלות:** האדמין אחראי בלעדית על סכמה, RLS, Edge Functions, RPC ו-cron.
הקליינט **לא** מריץ DDL/DML על פרוד — ולכן כל פריט כאן חוסם עד שהאדמין מטפל.

> מסמך חי. כל תיקון בקליינט שנוגע בנתונים שהאדמין קורא, תלוי במדיניות/Edge
> בבעלותו, או משנה חוזה משותף — חייב להופיע כאן לפני שחרור.

## 📬 סטטוס מול האדמין (עודכן 11.09.2026)

| פריט | מצב | הערות |
|---|---|---|
| S1 | ✅ אושר ע"י האדמין, בתכנון | יוסיף עמודת `deleted_by` (`'self'`/`'business'`) — `name=''` לא אמין כסימן (לקוח שהוקם בלי שם ונמחק ע"י העסק נראה זהה). ה-RPC ישחזר רק `deleted_by='self'` בתוך החלון. ישלח חוזה מדויק (שם, פרמטרים, החזרה). **הקליינט לא ימזג זרימת שחזור עד אז.** הוצע: `deleted_by IS NULL` (שורות קיימות) = לא ניתן לשחזור |
| S1 — באג אצל האדמין | 🐛 נמצא ע"י האדמין | `LogsManager.restoreCustomer` מוחק blacklist לפי טלפון מדויק בלבד, והחסימה נשמרת ב-972 ⇒ גם שחזור מהאדמין משאיר לקוח עם 05 חסום. יתוקן במסגרת S1 |
| S2 | ⏳ **ממתין להחלטת המשתמש** | המלצת שני הסוכנים: לקוח שהעסק מחק חוזר רק דרך מסך השחזור באדמין |
| S3, S4, S7, S9 | ⏳ ברשימת האדמין | יחזור בנפרד |
| S5 | ⛔ לא ניתן לאמת | דורש כתיבה — אף סוכן לא יכול. דרוש המשתמש (Dashboard) |
| חסם תשתיתי | ⛔ | ה-Supabase MCP של האדמין מחזיר permission denied (גם בקריאה) ⇒ אין לו אימות פרוד. כל שינוי DB עובר Change Gate + אישור משתמש ⇒ **אין ETA** |

⚠️ **תוקן 11.09.2026 — השורה המחוקה היחידה בפרוד היא כנראה מחיקת עסק, לא מחיקה עצמית.**
נאמר קודם לאדמין שבעסק 0022 יש "לקוח שמחק את עצמו" — זה לא אומת. בדיקה (ספירה לפי
תנאי על השם, בלי לשלוף אותו): השורה היחידה עם `deleted_at` היא עם **שם לא-ריק**, בעוד
ששני מסלולי המחיקה העצמית (`rpc('customer_self_delete')` שהאפליקציה קוראת ב-PunchCard.tsx,
וה-Edge `customer-self-delete`) מבצעים `name=''`. הומלץ לאדמין **לא** לסמן אותה
`deleted_by='self'` (זה היה הופך לקוח שהעסק מחק לניתן לשחזור מהאפליקציה — סותר את S2),
אלא להשאיר `NULL`. דחיפות ה-21.09 מתבטלת: אין לקוח שממתין לשחזור עצמי.

---

## 🔴 פריטים שחוסמים תיקון בקליינט (הקליינט **לא** שינה — ממתין לאדמין)

### S1 · שחזור לקוח שמחק את עצמו — נדרש endpoint בבעלות האדמין
**מצב היום:** לקוח שמחק את עצמו וחוזר בתוך 30 יום נתקע. ההרשמה מחדש לא מאפסת
`deleted_at`, והכרטיסייה הישנה עדיין `active` ⇒ "רישום כפול" או לולאה.

**למה הקליינט לא מתקן בעצמו** (נמצא בסקירה אדברסרית — תיקון נאיבי מזיק):
1. `customer_self_delete` מכניס שורות `push`+`sms` ל-`notifications_blacklist`
   (טלפון בפורמט 972). איפוס `deleted_at` בלבד משאיר את הלקוח **חסום לתמיד**
   מכל פוש ו-SMS ממוקדים (`fcm-dispatch`, `sms-dispatch`) — בשקט, בעוד שמסך
   ההעדפות בקליינט מציג "פעיל".
2. מחיקת לקוח **מהאדמין** (`handleSoftDeleteCustomer`) לא מסמנת כרטיסיות — כך
   שמבחינת הקליינט לקוח שהעסק מחק נראה זהה ללקוח שמחק את עצמו. איפוס גורף היה
   מאפשר לכל מי שהעסק הסיר לבטל את ההסרה בהרשמה מחדש.
3. כל מי שמקליד מספר של לקוח שמחק את עצמו היה מבטל את המחיקה שלו.
4. השחזור של האדמין (`LogsManager.restoreCustomer`) רושם `restore_customer`;
   שחזור מהקליינט לא היה משאיר שום תיעוד.

**נדרש:** RPC/Edge בבעלות האדמין, למשל `customer_self_restore(business_code, phone)`,
שמשקף את `LogsManager.restoreCustomer`:
- משחזר **רק** שורות עם חתימת מחיקה עצמית (`name = ''`) ורק בתוך חלון ה-30 יום;
- מאפס `deleted_at` + `hard_delete_after`;
- מוחק את שורות ה-blacklist בשני הערוצים, **בשני פורמטי הטלפון** (05/972)
  ובשני פורמטי קוד העסק (מרופד/לא מרופד);
- רושם `activity_logs` עם `action_type='restore_customer'`, `source='mobile'`;
- מחליט מה עושים בכרטיסייה הישנה.

אחרי שה-endpoint קיים — הקליינט יקרא לו במקום לכתוב ישירות.

### S2 · לקוחות שהעסק מחק — החלטה מדינית
האם לקוח **שהעסק מחק** רשאי אי-פעם לחזור דרך האפליקציה? ההמלצה: **לא** —
רק דרך מסך השחזור של האדמין. נדרשת החלטה מפורשת לפני S1.

### S3 · ה-GC משאיר כרטיסיות יתומות
`hard-delete-gc` מוחק רק מ-`customers` (`.lt('hard_delete_after', now)`), לא את
`PunchCards`. אחרי 30 יום נשארות כרטיסיות `active` בלי לקוח. אותו מספר שנרשם שוב
מקבל `card_number` זהה (עסק+טלפון+מוצר) ⇒ **"רישום כפול" לתמיד** — הבעיה ממשיכה
גם אחרי תקופת השחזור. גם שורות ה-blacklist נשארות.
**נדרש:** ה-GC ימחק/יסמן `status='deleted'` גם את הכרטיסיות, ויסיר את שורות ה-blacklist.

### S4 · קודי הזמנה (רפרלים) — לא עבדו מעולם, והתיקון הנאיבי מסוכן
**אומת בפרוד:** `business_code` שמור תמיד בפורמט `"0021"`, והחיפוש ב-
`newclient_form` חותך אפסים מובילים (`"0021"` → `"21"`) ⇒ **0 תוצאות תמיד**.
אף קוד הזמנה לא קיבל קרדיט מאז שהפיצ'ר נבנה.

**למה הקליינט לא מתקן** — הפעלת החיפוש חושפת אלגוריתם שבור:
- ההתאמה לפי 4 ספרות אחרונות של טלפון בלבד (4 הספרות האקראיות בקוד לא נשמרות
  בשום מקום) ⇒ התנגשויות כמעט ודאיות בעסק עם מאות לקוחות, והמזמין נבחר שרירותית;
- קוד קצר ⇒ `like '%'` ⇒ **כל** לקוח בעסק מתאים;
- אין בדיקה שקוד העסק בקוד = העסק שנבחר; הפניה עצמית אפשרית; שורה בכל ניסיון;
- הדשבורד סופר רק `status='completed'`, הקליינט מכניס `'pending'`, ואף קוד לא מקדם.
- לפי RLS_CHANGE_LEDGER, `referrals` מתוכננת להפוך ל-Edge-only.
**נדרש:** החלטת עיצוב (לשמור את קוד ההזמנה המלא בעת יצירתו / Edge שמאמת) לפני תיקון.

### S5 · מדיניות ה-UPDATE על `customers` — לא ניתן לאמת מהקליינט
הקליינט לא יכול לבדוק כתיבות מול פרוד. רלוונטי לכל שחזור עתידי (S1).

---

## 🟡 שינויים שהקליינט **כן** ביצע ומשנים נתונים שהאדמין רואה

### S6 · שם לקוח קיים לא נדרס יותר (PR #11)
**לפני:** הקלדת מספר של לקוח קיים בטופס הרישום הריצה UPDATE שהחליף את **שמו**
בדשבורד. **אחרי:** אם קיימת רשומה עם שם מלא ושונה — `name` לא נשלח ב-upsert.
רשומה בלי שם, או עם אותו שם — מתעדכנת כרגיל.

### S7 · לא נוצרות יותר רשומות לקוח יתומות (PR #11)
**לפני:** בדיקת פורמט הטלפון רצה **אחרי** ה-upsert, כך שכל ניסיון עם מספר פגום
השאיר רשומת לקוח בלי כרטיסייה בדשבורד. **אחרי:** הבדיקה לפני כל כתיבה.
רשומות יתומות קיימות — **לא נוקו**, ייתכן שכדאי לנקות מצד האדמין.

### S8 · ניקוב ללקוחות בפורמט 972 (PR #11)
**לפני:** `fetchCustomerCards` השתמשה בהתאמה מדויקת, כך שלקוח שהוקם באדמין
בפורמט `972...` ראה את הכרטיסייה שלו אך לא יכל לנקב ב-NFC.
**אחרי:** חיפוש בווריאנטים. צפויות **בקשות ניקוב חדשות** בתור האישורים מלקוחות
שלא יכלו לנקב עד היום. הטלפון בבקשה נשאר בפורמט 05 (ה-Edge מנרמל ומשווה).

---

## ℹ️ לידיעת האדמין — פער בין תיעוד לפרוד

### S9 · קובץ ה-RLS בריפו לא תואם לפרוד
`OLD_DEV_DOCS_STORAGE/sql/add_customers_rls_soft_delete.sql` מגדיר policy
`Exclude soft-deleted customers` (`using (deleted_at is null)`).
**אומת בפרוד (קריאה בלבד):** שורה עם `deleted_at` **גלויה** ל-anon ⇒ ה-policy
**לא מופעל**. אין לכך השפעה על הקליינט היום (הוא מסנן בעצמו), אבל התיעוד מטעה.

### S10 · מוצרים שכבר קיימים ללקוח (נדחה)
סינון המוצרים בבורר חייב לשקף את כלל האדמין לגבי אילו כרטיסיות חוסמות חדשה
(פגה/מלאה/נמחקה). נדחה עד שהכלל מוגדר.
